<Window x:Class="VamPackageUpdater.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="VAM Package Updater" Height="760" Width="760"
        Icon="Assets/icon.png"
        WindowStartupLocation="CenterScreen"
        Background="#1e1e1e" Foreground="#e0e0e0"
        FontFamily="Segoe UI" FontSize="13">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#e0e0e0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2d2d2d"/>
            <Setter Property="Foreground" Value="#e0e0e0"/>
            <Setter Property="BorderBrush" Value="#3f3f3f"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="CaretBrush" Value="#e0e0e0"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#2d2d2d"/>
            <Setter Property="Foreground" Value="#e0e0e0"/>
            <Setter Property="BorderBrush" Value="#3f3f3f"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#3a3a3a"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#5a5a5a"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#252525"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Background" Value="#222"/>
                                <Setter Property="Foreground" Value="#666"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#2d2d2d"/>
            <Setter Property="Foreground" Value="#e0e0e0"/>
            <Setter Property="BorderBrush" Value="#3f3f3f"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,3"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="22"/>
                            </Grid.ColumnDefinitions>
                            <Border x:Name="Bd" Grid.ColumnSpan="2"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"/>
                            <ToggleButton Grid.ColumnSpan="2"
                                          Background="Transparent"
                                          BorderThickness="0"
                                          Focusable="False"
                                          ClickMode="Press"
                                          IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="Transparent">
                                            <Path HorizontalAlignment="Right" VerticalAlignment="Center"
                                                  Margin="0,0,8,0"
                                                  Fill="#c0c0c0"
                                                  Data="M 0 0 L 4 4 L 8 0 Z"/>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter Grid.Column="0"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                              Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"
                                              IsHitTestVisible="False"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                            <Popup Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Border Background="#2d2d2d"
                                        BorderBrush="#3f3f3f"
                                        BorderThickness="1"
                                        MinWidth="{TemplateBinding ActualWidth}"
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <ScrollViewer>
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#5a5a5a"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#2d2d2d"/>
            <Setter Property="Foreground" Value="#e0e0e0"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#3a3a3a"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DataGrid dark theme -->
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#1a1a1a"/>
            <Setter Property="Foreground" Value="#e0e0e0"/>
            <Setter Property="BorderBrush" Value="#3f3f3f"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="#1a1a1a"/>
            <Setter Property="AlternatingRowBackground" Value="#222"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#2a2a2a"/>
            <Setter Property="VerticalGridLinesBrush" Value="#2a2a2a"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#2d2d2d"/>
            <Setter Property="Foreground" Value="#e0e0e0"/>
            <Setter Property="BorderBrush" Value="#3f3f3f"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="Foreground" Value="#e0e0e0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#3a5075"/>
                    <Setter Property="Foreground" Value="#ffffff"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="#1a1a1a"/>
            <Setter Property="Foreground" Value="#e0e0e0"/>
        </Style>

        <!-- Dark scrollbar -->
        <Style x:Key="DarkScrollBarThumb" TargetType="Thumb">
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border x:Name="Bd" Background="#3f3f3f" CornerRadius="3" Margin="2"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#5a5a5a"/>
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#6a6a6a"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <ControlTemplate x:Key="DarkScrollBarVertical" TargetType="ScrollBar">
            <Grid Background="#141414">
                <Track Name="PART_Track" IsDirectionReversed="True">
                    <Track.Thumb>
                        <Thumb Style="{StaticResource DarkScrollBarThumb}"/>
                    </Track.Thumb>
                    <Track.IncreaseRepeatButton>
                        <RepeatButton Opacity="0" Command="ScrollBar.PageDownCommand"/>
                    </Track.IncreaseRepeatButton>
                    <Track.DecreaseRepeatButton>
                        <RepeatButton Opacity="0" Command="ScrollBar.PageUpCommand"/>
                    </Track.DecreaseRepeatButton>
                </Track>
            </Grid>
        </ControlTemplate>
        <ControlTemplate x:Key="DarkScrollBarHorizontal" TargetType="ScrollBar">
            <Grid Background="#141414">
                <Track Name="PART_Track">
                    <Track.Thumb>
                        <Thumb Style="{StaticResource DarkScrollBarThumb}"/>
                    </Track.Thumb>
                    <Track.IncreaseRepeatButton>
                        <RepeatButton Opacity="0" Command="ScrollBar.PageRightCommand"/>
                    </Track.IncreaseRepeatButton>
                    <Track.DecreaseRepeatButton>
                        <RepeatButton Opacity="0" Command="ScrollBar.PageLeftCommand"/>
                    </Track.DecreaseRepeatButton>
                </Track>
            </Grid>
        </ControlTemplate>
        <Style TargetType="ScrollBar">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Vertical">
                    <Setter Property="Width" Value="10"/>
                    <Setter Property="MinWidth" Value="10"/>
                    <Setter Property="Template" Value="{StaticResource DarkScrollBarVertical}"/>
                </Trigger>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Height" Value="10"/>
                    <Setter Property="MinHeight" Value="10"/>
                    <Setter Property="Template" Value="{StaticResource DarkScrollBarHorizontal}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Filter tab style (looks like a pill-button; active = highlighted) -->
        <Style x:Key="FilterTabStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#a0a0a0"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
            <Setter Property="Padding" Value="12,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#2a2a2a"/>
                                <Setter Property="Foreground" Value="#e0e0e0"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#5a7aaa"/>
                                <Setter Property="Foreground" Value="#e0e0e0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- CheckBox dark style -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#e0e0e0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="6,0,0,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="Box" Width="16" Height="16"
                                    Background="#1a1a1a"
                                    BorderBrush="#5a5a5a"
                                    BorderThickness="1"
                                    VerticalAlignment="Center"
                                    SnapsToDevicePixels="True">
                                <Path x:Name="Check"
                                      Data="M 2.5 6 L 6 9.5 L 12.5 3"
                                      Stroke="#e0e0e0"
                                      StrokeThickness="2"
                                      StrokeStartLineCap="Round"
                                      StrokeEndLineCap="Round"
                                      Visibility="Collapsed"/>
                            </Border>
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="Center"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Check" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="Box" Property="Background" Value="#2a4a7a"/>
                                <Setter TargetName="Box" Property="BorderBrush" Value="#5a7aaa"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Box" Property="BorderBrush" Value="#8a8a8a"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- GridSplitter -->
        <Style TargetType="GridSplitter">
            <Setter Property="Background" Value="#2a2a2a"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Height" Value="5"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Cursor" Value="SizeNS"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#5a5a5a"/>
                </Trigger>
                <Trigger Property="IsDragging" Value="True">
                    <Setter Property="Background" Value="#6a6a6a"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Section tabs (Plugins / Voxta Resources) -->
        <Style x:Key="SectionTabStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#a0a0a0"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0,0,0,3"/>
            <Setter Property="Padding" Value="16,7"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#2a2a2a"/>
                                <Setter Property="Foreground" Value="#e0e0e0"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#4EC9B0"/>
                                <Setter Property="Foreground" Value="#ffffff"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="3*" MinHeight="120"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*" MinHeight="160"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="1. Select .var package" FontWeight="Bold" Margin="0,0,0,4"/>
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="FilePathBox" Grid.Column="0" IsReadOnly="True" Margin="0,0,6,0"/>
            <Button Grid.Column="1" Content="Browse..." Click="BrowseButton_Click" MinWidth="90"/>
        </Grid>

        <Border Grid.Row="2" x:Name="MetaPanel" Visibility="Collapsed"
                Background="#171717" BorderBrush="#2a2a2a" BorderThickness="1"
                Padding="12,10" Margin="0,0,0,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="90"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="70"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Creator:" Foreground="#808080"/>
                <TextBlock Grid.Row="0" Grid.Column="1" x:Name="MetaCreator" Foreground="#4EC9B0" FontFamily="Cascadia Mono, Consolas"/>
                <TextBlock Grid.Row="0" Grid.Column="2" Text="Size:" Foreground="#808080" Margin="10,0,0,0"/>
                <TextBlock Grid.Row="0" Grid.Column="3" x:Name="MetaSize" Foreground="#CE9178" FontFamily="Cascadia Mono, Consolas"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="License:" Foreground="#808080" Margin="0,4,0,0"/>
                <TextBlock Grid.Row="1" Grid.Column="1" x:Name="MetaLicense" Foreground="#DCDCAA" FontFamily="Cascadia Mono, Consolas" Margin="0,4,0,0"/>
                <TextBlock Grid.Row="1" Grid.Column="2" Text="Files:" Foreground="#808080" Margin="10,4,0,0"/>
                <TextBlock Grid.Row="1" Grid.Column="3" x:Name="MetaFileCount" Foreground="#CE9178" FontFamily="Cascadia Mono, Consolas" Margin="0,4,0,0"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Description:" Foreground="#808080" Margin="0,4,0,0" VerticalAlignment="Top"/>
                <TextBlock Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="3" x:Name="MetaDescription"
                           Foreground="#c0c0c0" TextWrapping="Wrap" Margin="0,4,0,0"
                           MaxHeight="60" TextTrimming="CharacterEllipsis"/>
            </Grid>
        </Border>

        <Grid Grid.Row="3" Margin="0,0,0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="2. Edit" FontWeight="Bold"/>
            <Button Grid.Column="1" Content="Rescan" Click="RescanButton_Click"
                    IsEnabled="False" x:Name="RescanButton" MinWidth="80" Padding="8,3"/>
        </Grid>

        <Border Grid.Row="4" BorderBrush="#2a2a2a" BorderThickness="0,0,0,1" Margin="0,2,0,0">
            <StackPanel Orientation="Horizontal" x:Name="SectionTabStrip">
                <ToggleButton x:Name="SectionTabPlugins" Content="Plugin references"
                              IsChecked="True" Click="SectionTab_Click" Tag="Plugins"
                              Style="{StaticResource SectionTabStyle}"/>
                <ToggleButton x:Name="SectionTabVoxta" Content="Voxta resources"
                              Click="SectionTab_Click" Tag="Voxta"
                              Style="{StaticResource SectionTabStyle}"/>
                <ToggleButton x:Name="SectionTabHubDeps" Content="Download deps"
                              Click="SectionTab_Click" Tag="HubDeps"
                              Style="{StaticResource SectionTabStyle}"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="5" Margin="0,0,0,6">
            <!-- PLUGINS SECTION -->
            <Grid x:Name="PluginsSection">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,6,0,2">
                    <Button Content="Set all to latest" Click="SetAllLatestButton_Click"
                            IsEnabled="False" x:Name="SetAllLatestButton" Margin="0,0,6,0" Padding="8,3"/>
                    <Button Content="Clear all" Click="ClearAllButton_Click"
                            IsEnabled="False" x:Name="ClearAllButton" Padding="8,3"/>
                </StackPanel>

                <Border Grid.Row="1" BorderBrush="#2a2a2a" BorderThickness="0,0,0,1" Margin="0,4,0,6">
                    <StackPanel Orientation="Horizontal" x:Name="FilterTabStrip">
                        <ToggleButton x:Name="TabAll" Content="All" Tag="{x:Null}"
                                      IsChecked="True" Click="FilterTab_Click"
                                      Style="{StaticResource FilterTabStyle}"/>
                        <ToggleButton x:Name="TabPlugins" Content="Plugins" Tag="Plugin"
                                      Click="FilterTab_Click" Style="{StaticResource FilterTabStyle}"/>
                        <ToggleButton x:Name="TabClothing" Content="Clothing" Tag="Clothing"
                                      Click="FilterTab_Click" Style="{StaticResource FilterTabStyle}"/>
                        <ToggleButton x:Name="TabHair" Content="Hair" Tag="Hair"
                                      Click="FilterTab_Click" Style="{StaticResource FilterTabStyle}"/>
                        <ToggleButton x:Name="TabAppearance" Content="Appearance" Tag="Appearance"
                                      Click="FilterTab_Click" Style="{StaticResource FilterTabStyle}"/>
                        <ToggleButton x:Name="TabMorph" Content="Morphs" Tag="Morph"
                                      Click="FilterTab_Click" Style="{StaticResource FilterTabStyle}"/>
                        <ToggleButton x:Name="TabTexture" Content="Textures" Tag="Texture"
                                      Click="FilterTab_Click" Style="{StaticResource FilterTabStyle}"/>
                        <ToggleButton x:Name="TabAsset" Content="Assets" Tag="Asset"
                                      Click="FilterTab_Click" Style="{StaticResource FilterTabStyle}"/>
                        <ToggleButton x:Name="TabOther" Content="Other" Tag="Other"
                                      Click="FilterTab_Click" Style="{StaticResource FilterTabStyle}"/>
                    </StackPanel>
                </Border>

                <DataGrid Grid.Row="2" x:Name="PluginsGrid">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Plugin" Binding="{Binding PluginId}" IsReadOnly="True" Width="2*"/>
                        <DataGridTextColumn Header="Current" Binding="{Binding CurrentVersionsDisplay}" IsReadOnly="True" Width="90"/>
                        <DataGridTextColumn Header="Refs" Binding="{Binding OccurrenceCount}" IsReadOnly="True" Width="55"/>
                        <DataGridTemplateColumn Header="New Version" Width="200" SortMemberPath="NewVersion">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid Margin="4,3">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBox Grid.Column="0"
                                                 Text="{Binding NewVersion, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                                 Background="#0f0f0f"
                                                 Foreground="#e0e0e0"
                                                 CaretBrush="#e0e0e0"
                                                 BorderBrush="#4a4a4a"
                                                 BorderThickness="1"
                                                 Padding="5,3"
                                                 VerticalContentAlignment="Center"
                                                 FontFamily="Cascadia Mono, Consolas"
                                                 GotFocus="VersionTextBox_GotFocus"
                                                 PreviewMouseLeftButtonDown="VersionTextBox_PreviewMouseLeftButtonDown"/>
                                        <Button Grid.Column="1" Content="latest"
                                                Click="SetRowLatestButton_Click"
                                                Tag="{Binding}"
                                                Margin="4,0,0,0" Padding="6,2"
                                                FontSize="11"/>
                                    </Grid>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>

            <!-- VOXTA RESOURCES SECTION -->
            <Grid x:Name="VoxtaSection" Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,6,0,2">
                    <Button Content="Clear all attachments" Click="ClearAllAttachmentsButton_Click"
                            IsEnabled="False" x:Name="ClearAllAttachmentsButton" Padding="8,3"/>
                </StackPanel>

                <TextBlock Grid.Row="1" x:Name="VoxtaSectionHelp"
                           Foreground="#808080" FontSize="12" Margin="0,6,0,8" TextWrapping="Wrap"
                           Text="Scene-referenced Voxta resources. For every &quot;Missing&quot; row, attach the exported PNG from Voxta Studio — it will be embedded inside the .var when you apply."/>

                <DataGrid Grid.Row="2" x:Name="VoxtaGrid">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Kind" Binding="{Binding KindLabel}" IsReadOnly="True" Width="110"/>
                        <DataGridTextColumn Header="Name / UUID" Binding="{Binding NameOrId}" IsReadOnly="True" Width="2*"/>
                        <DataGridTemplateColumn Header="Status" Width="110">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding StatusLabel}" FontWeight="SemiBold" VerticalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="#c0c0c0"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding StatusLabel}" Value="Bundled">
                                                        <Setter Property="Foreground" Value="#6A9955"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding StatusLabel}" Value="Missing">
                                                        <Setter Property="Foreground" Value="#F48771"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding StatusLabel}" Value="Attached">
                                                        <Setter Property="Foreground" Value="#569CD6"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding StatusLabel}" Value="Replacing">
                                                        <Setter Property="Foreground" Value="#D7BA7D"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding StatusLabel}" Value="Orphan">
                                                        <Setter Property="Foreground" Value="#808080"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Attach PNG" Width="290">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid Margin="4,3">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0"
                                                   Text="{Binding AttachedFileName}"
                                                   ToolTip="{Binding AttachedTooltip}"
                                                   Foreground="#569CD6"
                                                   FontFamily="Cascadia Mono, Consolas"
                                                   VerticalAlignment="Center"
                                                   TextTrimming="CharacterEllipsis"/>
                                        <Button Grid.Column="1" Content="Browse..."
                                                Click="AttachResourceButton_Click"
                                                Tag="{Binding}"
                                                Margin="4,0,0,0" Padding="8,2"
                                                FontSize="11"
                                                ToolTip="Pick any PNG exported from Voxta Studio — filename doesn't matter, it will be renamed to the Voxta resource UUID when embedded."/>
                                        <Button Grid.Column="2" Content="Clear"
                                                Click="DetachResourceButton_Click"
                                                Tag="{Binding}"
                                                Margin="4,0,0,0" Padding="8,2"
                                                FontSize="11"
                                                ToolTip="Remove the staged attachment">
                                            <Button.Style>
                                                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding HasAttachment}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </Grid>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>

            <!-- HUB DEPENDENCIES SECTION -->
            <Grid x:Name="HubDepsSection" Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,6,0,2">
                    <Button Content="Download all" Click="DownloadAllMissingButton_Click"
                            IsEnabled="False" x:Name="DownloadAllMissingButton" Padding="8,3"
                            ToolTip="Download every 'Missing' and 'Update avail' package from hub.virtamate.com. Existing installed versions are left in place - VaM resolves .latest to the newest one present."/>
                </StackPanel>

                <TextBlock Grid.Row="1"
                           Foreground="#808080" FontSize="12" Margin="0,6,0,8" TextWrapping="Wrap"
                           Text="Packages this .var depends on (from meta.json, transitive). 'Download all' fetches 'Missing' and 'Update avail' rows from hub.virtamate.com. Paid / non-hub content can't be auto-downloaded — grab those manually from the creator's Patreon."/>

                <DataGrid Grid.Row="2" x:Name="HubDepsGrid">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Package" Binding="{Binding Name}" IsReadOnly="True" Width="3*"/>
                        <DataGridTextColumn Header="License" Binding="{Binding LicenseLabel}" IsReadOnly="True" Width="100"/>
                        <DataGridTemplateColumn Header="Status" Width="140">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding StatusLabel}" FontWeight="SemiBold"
                                                   ToolTip="{Binding StatusTooltip}">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#c0c0c0"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding StatusLabel}" Value="Installed">
                                                            <Setter Property="Foreground" Value="#6A9955"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding StatusLabel}" Value="Update avail">
                                                            <Setter Property="Foreground" Value="#D7BA7D"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding StatusLabel}" Value="Wrong version">
                                                            <Setter Property="Foreground" Value="#D7BA7D"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding StatusLabel}" Value="Missing">
                                                            <Setter Property="Foreground" Value="#F48771"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding StatusLabel}" Value="Queued">
                                                            <Setter Property="Foreground" Value="#808080"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding StatusLabel}" Value="Downloading">
                                                            <Setter Property="Foreground" Value="#569CD6"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding StatusLabel}" Value="Downloaded">
                                                            <Setter Property="Foreground" Value="#6A9955"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding StatusLabel}" Value="Not on Hub">
                                                            <Setter Property="Foreground" Value="#D7BA7D"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding StatusLabel}" Value="Error">
                                                            <Setter Property="Foreground" Value="#F48771"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Grid>

        <GridSplitter Grid.Row="6" ResizeDirection="Rows" ResizeBehavior="PreviousAndNext" Margin="0,2,0,2"/>

        <Grid Grid.Row="7">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="3. Options" FontWeight="Bold" Margin="0,0,0,4"/>
            <Grid Grid.Row="1" Margin="0,0,0,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="110"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Change license:"/>
                <ComboBox Grid.Row="0" Grid.Column="1" x:Name="LicenseCombo"/>
                <CheckBox Grid.Row="1" Grid.Column="1" x:Name="DryRunCheck"
                          Content="Preview changes only (dry run — don't write the .var)"
                          Margin="0,8,0,0"/>
            </Grid>

            <Button Grid.Row="2" x:Name="UpdateButton" Content="Update Package"
                    Click="UpdateButton_Click" IsEnabled="False"
                    Padding="0,8" FontWeight="Bold" Margin="0,4,0,8"/>

            <TextBlock Grid.Row="3" Text="Log" FontWeight="Bold" Margin="0,0,0,4"/>
            <Border Grid.Row="4" BorderBrush="#3f3f3f" BorderThickness="1" Background="#141414">
                <RichTextBox x:Name="LogBox" IsReadOnly="True" BorderThickness="0"
                             Background="Transparent" Foreground="#c0c0c0"
                             FontFamily="Cascadia Mono, Consolas" FontSize="12"
                             Padding="8"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto"
                             IsDocumentEnabled="False"
                             IsUndoEnabled="False"/>
            </Border>
        </Grid>
    </Grid>
</Window>
